# Signature Analyzer AI
Proyecto demo: pipeline para entrenar un CNN que detecte alteraciones en firmas y API web para predecir.

Instrucciones:
- pip install -r requirements.txt
- python model/train.py  # para entrenar (usa dataset local)
- python web/app.py     # para servir la API
